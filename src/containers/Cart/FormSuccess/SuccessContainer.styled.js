import styled from "styled-components";
import PageContainer from "../../PageContainer.styled.js";

const SuccessContainer = styled(PageContainer)`
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;

  p {
    font-size: 18px;
  }
`;

export default SuccessContainer;
